// Minimal workspace anchor — getLaceDir() on the extension side walks
// up from here to discover the project root. Empty content keeps the
// terraform fmt/validate commands quiet while still giving commands
// a concrete path to operate on.
