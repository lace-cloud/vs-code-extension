{
  "name": "@lace-cloud/host-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "VS Code extension host smoke + command tests (Plan 2 Phase G).",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf out",
    "pretest": "pnpm run clean && pnpm run build",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@lace-cloud/host": "0.6.5",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.96.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "mocha": "^11.7.4",
    "typescript": "^5.6.3"
  }
}
